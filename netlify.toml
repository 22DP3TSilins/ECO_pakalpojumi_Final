[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so Vue Router routes (/products, /profile, etc.) don't 404 on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
